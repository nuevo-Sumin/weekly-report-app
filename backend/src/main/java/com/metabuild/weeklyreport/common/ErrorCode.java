package com.metabuild.weeklyreport.common;

public enum ErrorCode {
    DUPLICATE_LOGIN_ID,
    INVALID_CREDENTIALS,
    USER_NOT_FOUND,
    INACTIVE_USER,
    INVALID_TOKEN,
    UNAUTHORIZED,
    FORBIDDEN
}
